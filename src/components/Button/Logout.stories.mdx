import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import Logout from "./index.jsx";

<Meta title="Components/Button" component={Logout} />

# Logout Button

<ArgsTable of={Logout} />

<Canvas>
  <Story
    name="Logout Component"
    args={{
      ...Logout.defaultProps,
      text: "Logout",
      className: "logout-button",
    }}
  >
    {(args) => <Logout {...args} />}
  </Story>
</Canvas>
